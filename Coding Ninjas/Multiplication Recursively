import sys 
sys.setrecursionlimit(10**6)


def mul(m,n):
    if n==0:
        return 0
    if n == 1:
        return m
    else:
        return m+ mul(m,n-1)
    
    
m=int(input())
n=int(input())
print(mul(m,n))
